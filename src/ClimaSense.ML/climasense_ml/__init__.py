"""ClimaSense ML tier.

Slice 1 only ships the scaffolding: clock + cursor + logging + healthchecks.
Later slices will populate the ingestion / forecasting / analytics surface.
"""

__version__ = "0.1.0"
